version: '3.8'

services:
  website:
    context: ./
    build: website
    restart: always
    environment:
      MODULE_NAME: manage
    ports:
    - 80:80
    depends_on:
      - database

  database:
    build: database
    restart: always
    ports:
    - 5432:5432
